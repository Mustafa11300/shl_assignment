services:
  - type: web
    name: shl-assessment-recommender
    runtime: docker
    plan: free
    healthCheckPath: /health
    envVars:
      - key: USE_LLM
        value: "false"
      - key: PORT
        value: 8000
